<h1><%= t '.title' %> </h1>
<% @users.each do |user| %>
  <li>
    <%=  link_to user.username, user %>
  </li>
<% end %>

<%= paginate @users %>
<p><%= link_to t('views.common.back'), books_path %></p>
